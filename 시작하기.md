# 시작하기 — 웹사이트(Claude / ChatGPT)에서 사용하기

> 파일 목록과 설명은 `파일설명.md`를 참고하세요.
> 이 저장소는 원래 Claude Code(CLI 에이전트) 전용입니다. 웹 Project에서는 파일 실행 없이 **대화형 프레임워크**로 씁니다 — 도구 격차는 `지침.md` 2번 참고.

---

## 1단계 — Project 만들고 기반 파일 올리기

### Claude.ai (Projects)

1. Claude.ai에서 새 Project 생성
2. **Project 지침(instructions)** 칸에 `지침.md` 전체 내용 붙여넣기
3. **Project 지식(knowledge)**에 아래 3개 파일 첨부
   - `_shared/ROUTING.md`
   - `_shared/CORE-LAWS.md`
   - `_shared/DEFERRED.md`
4. (선택) Claude 계정 메모리(Settings → Memory)에 `메모리.md` 내용 저장 — 다른 Project/대화에서도 이 프로젝트 성격을 기억하게 하고 싶을 때

### ChatGPT (Projects)

1. ChatGPT에서 새 Project 생성
2. **프로젝트 지침** 칸에 `지침.md` 전체 내용 붙여넣기
3. **파일** 탭에 위와 동일하게 `_shared/ROUTING.md`, `_shared/CORE-LAWS.md`, `_shared/DEFERRED.md` 업로드
4. (선택) "ChatGPT가 기억했으면 하는 내용"에 `메모리.md` 요약을 저장

> 19개 스킬의 `SKILL.md`를 전부 한 번에 올릴 필요는 없습니다 — 컨텍스트만 늘어납니다. 기반 3개 파일만 올려두고, 실제로 쓸 스킬은 그때그때 2단계에서 추가합니다.

---

## 2단계 — genie로 라우팅 받기

새 대화(또는 기존 Project 대화)에서:

```
지니야, [하고 싶은 일을 편하게 설명]
```

```
예시:
"지니야, 새로 시작할 사업 아이디어를 리서치하고 검증해줘"
"지니야, 이 스크립트를 영상으로 만들고 싶어"
"지니야, 이 코드 세션을 어떻게 진행해야 할지 계획부터 잡아줘"
```

genie가 `_shared/ROUTING.md`를 기준으로 어떤 스킬이 맞는지 알려줍니다. 스킬 이름을 이미 알면 이 단계를 건너뛰고 바로 3단계로 가세요 (예: "biz-council로 이 아이디어 검증해줘").

해당하는 스킬이 없다고 하면, `_shared/DEFERRED.md`에 이미 검토·보류된 분야인지부터 확인해줍니다 — 근거 없이 "일단 해볼게요"로 넘어가지 않습니다.

---

## 3단계 — 해당 스킬 파일 첨부 후 실제 작업

1. 안내받은 스킬 폴더에서 `SKILL.md`를 열어 전체 복사
2. 같은 대화(또는 새 대화)에 붙여넣기 → 전송
3. 바로 이어서 실제 요청 입력

```
예시 (biz-council):
[biz-council/SKILL.md 전체 붙여넣기]

"1인 개발자를 위한 반려동물 건강관리 앱 아이디어를 리서치하고 검증해줘"
```

```
예시 (video-producer):
[video-producer/SKILL.md 전체 붙여넣기]

"아래 스크립트로 인트로 영상 구성만 짜줘 (실제 렌더링은 내가 로컬 hyperframes로 돌릴게)"
[스크립트 붙여넣기]
```

스킬이 다른 스킬을 참조하면(예: `curator` → `marketingskills`, `distribution` → `marketingskills`) 웹에서는 그 참조 저장소 파일을 직접 실행할 수 없으니, AI가 해당 방법론을 알고 있는 한도 내에서 텍스트로 대신 수행하고, 실제 코드/도구 실행이 필요한 부분은 "로컬 Claude Code에서 실행해야 한다"고 알려줄 것입니다.

---

## 스킬별 빠른 참고

| 하고 싶은 일 | 부를 스킬 |
|---|---|
| 새 사업/제품 아이디어 검증 | `biz-council` |
| 덱·랜딩페이지 디자인 방향, 보고서(DOCX/PPTX/PDF) | `design-report` |
| 자동화 에이전트/AI 워커 설계 | `agent-builder` |
| 마케팅/유통 채널 찾기 | `distribution` |
| 트렌드 리서치 + 콘텐츠 스크립트 | `curator` |
| 코딩 세션 계획/규율 세팅 | `vibe-coder` |
| 스크립트를 실제 영상으로 | `video-producer` |
| Claude/ChatGPT가 나를 계속 기억하게 하기 | `personal-memory` |
| 게임 제작 | `game-dev` |
| 기존 사업 재무/영업/운영 | `biz-ops` |
| 글쓰기를 사람 문체로 다듬기 | `writing` |
| SNS 카드뉴스 | `social-carousel` |
| 프롬프트 작성/개선 | `prompt-craft` |

전체 트리거 문구는 `_shared/ROUTING.md`, 스킬별 상세 입출력은 `USAGE.md` 참고.

---

## 자주 묻는 것

**Q. 검증 스크립트(`validate_workspace.ps1`)를 웹에서도 돌려야 하나요?**
아니요. 그건 git 저장소를 로컬에서 편집할 때만 필요합니다. 웹 Project 사용에는 무관합니다.

**Q. `biz-council`이 Reddit/X 데이터를 실시간으로 가져올 수 있나요?**
웹 Project 자체에는 그 도구(`WebSearch`, `last30days`)가 없습니다. 호스트가 자체 브라우징 기능을 제공하면 그걸로 대체 가능하지만, 없으면 실시간 검색 없이 진행한다는 걸 먼저 알려달라고 요청하세요 — 지어낸 데이터를 사실처럼 주지 않는 것이 이 프로젝트의 기본 원칙(`_shared/CORE-LAWS.md` LAW 0)입니다.

**Q. 여러 스킬을 한 번에 첨부해도 되나요?**
가능하지만 권장하지 않습니다. `지침.md` 4번처럼 먼저 요청을 하나로 분류한 뒤, 필요한 스킬 1개만 붙여넣는 편이 컨텍스트도 아끼고 결과도 더 정확합니다.

**Q. 아직 없는 분야(기획/제조/판매/재무운영 등)를 요청했어요.**
`_shared/DEFERRED.md`에 이미 검토·보류된 근거가 있으면 AI가 그대로 알려줍니다. 새로 만들고 싶다면 로컬 Claude Code 환경에서 `genie/SKILL.md`가 안내하는 절차(GitHub 실제 후보 조사 → 라이선스 확인 → 방법론 재작성 → `ROUTING.md` 등록)를 따라야 합니다 — 웹 Project만으로는 새 스킬을 "설치"할 수 없습니다.

**Q. Claude Code(로컬)와 결과가 다를 수 있나요?**
네. 로컬 Claude Code는 실제로 파일을 쓰고, 스크립트를 돌리고, 웹을 검색합니다. 웹 Project는 같은 방법론을 텍스트로만 따라가므로, 스크립트 실행이나 실시간 데이터가 꼭 필요한 단계는 결과가 더 제한적이거나 로컬 실행을 권유받을 수 있습니다.
