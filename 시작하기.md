# 시작하기 — 웹사이트(Claude / ChatGPT)에서 사용하기

> 파일 목록과 설명은 `파일설명.md`를 참고하세요.
> 이 안내는 Claude Code용 스킬 파일을 웹 Project의 문맥으로 첨부해 사용하는 방법입니다. 검색·파일 생성·코드 실행 가능 여부는 현재 제품과 설정에 따라 확인합니다 — 도구 격차는 `지침.md` 2번 참고.

---

## 1단계 — Project 만들고 기반 파일 올리기

### Claude.ai (Projects)

1. Claude.ai에서 새 Project 생성
2. **Project 지침(instructions)** 칸에 `지침.md` 전체 내용 붙여넣기
3. **Project 지식(knowledge)**에 아래 3개 파일 첨부
   - `_shared/ROUTING.md`
   - `_shared/CORE-LAWS.md`
   - `_shared/DEFERRED.md`
4. (선택) 요약이 필요하면 `메모리.md`를 참고하세요. 메모리는 프로젝트 지침과 기반 파일을 대신하지 않으며, Claude의 프로젝트 메모리는 프로젝트별로 분리됩니다.

### ChatGPT (Projects)

1. ChatGPT에서 새 Project 생성
2. **프로젝트 지침** 칸에 `지침.md` 전체 내용 붙여넣기
3. **파일/소스** 영역에 위와 동일하게 `_shared/ROUTING.md`, `_shared/CORE-LAWS.md`, `_shared/DEFERRED.md` 업로드
4. (선택) 요약이 필요하면 `메모리.md`를 참고하세요. 계정 메모리 설정을 바꾸지 않아도 위 지침과 파일로 시작할 수 있습니다.

> 25개 전문 스킬의 `SKILL.md`를 전부 한 번에 올릴 필요는 없습니다 — 컨텍스트만 늘어납니다. 기반 3개 파일만 올려두고, 실제로 쓸 스킬은 그때그때 3단계에서 추가합니다.

---

## 2단계 — 요청에 맞는 스킬 찾기

새 대화(또는 기존 Project 대화)에서:

```
지니야, [하고 싶은 일을 편하게 설명]
```

```
예시:
"지니야, 새로 시작할 사업 아이디어를 리서치하고 검증해줘"
"지니야, 이 스크립트를 영상으로 만들고 싶어"
"지니야, 이 코드 세션을 어떻게 진행해야 할지 계획부터 잡아줘"
```

Project가 `지침.md`의 라우팅 절차와 `_shared/ROUTING.md`를 기준으로 어떤 스킬이 맞는지 알려줍니다. 이 설정은 `genie/SKILL.md`를 자동 설치하는 것은 아닙니다. 스킬 이름을 이미 알면 이 단계를 건너뛰고 바로 3단계로 가세요 (예: "biz-council로 이 아이디어 검증해줘").

해당하는 스킬이 없다고 하면, `_shared/DEFERRED.md`에 이미 검토·보류된 분야인지부터 확인해줍니다 — 근거 없이 "일단 해볼게요"로 넘어가지 않습니다.

---

## 3단계 — 해당 스킬 파일 첨부 후 실제 작업

1. 안내받은 스킬의 `SKILL.md`를 첨부하거나 전체 내용을 붙여넣고 폴더 경로를 함께 적습니다.
2. 그 단계가 요구하는 보조 문서와 외부 스킬 원문도 첨부하거나, 접근 가능한 원문을 실제로 읽게 합니다. 예를 들어 `founder-finance`는 상위 스킬과 두 참조 문서가 필요합니다.
3. 같은 Project 대화에서 실제 요청을 입력합니다. 새 대화에서는 필요한 파일이 여전히 접근 가능한지 확인합니다.

```
예시 (biz-council):
[biz-council/SKILL.md 전체 붙여넣기]

"1인 개발자를 위한 반려동물 건강관리 앱 아이디어를 리서치하고 검증해줘"
```

```
예시 (video-producer):
[video-producer/SKILL.md 전체 붙여넣기]

"아래 스크립트로 인트로 영상 구성만 짜줘 (실제 렌더링은 내가 로컬 hyperframes로 돌릴게)"
[스크립트 붙여넣기]
```

스킬이 외부 스킬을 참조하면(예: `curator`·`distribution` → `marketingskills`), 해당 원문을 실제로 읽어야 합니다. 읽을 수 없으면 첨부를 요청하고 그 단계는 미확인으로 남깁니다. 파일 첨부만으로 로컬 경로나 외부 도구가 연결되지는 않으므로, 실행 환경이 필요한 단계는 사용 가능한 도구를 확인한 뒤 진행합니다.

---

## 스킬별 빠른 참고

| 하고 싶은 일 | 부를 스킬 |
|---|---|
| 새 사업/제품 아이디어 검증 | `biz-council` |
| 덱·랜딩페이지 디자인 방향, 보고서(DOCX/PPTX/PDF) | `design-report` |
| 자동화 에이전트/AI 워커 설계 | `agent-builder` |
| 마케팅/유통 채널 찾기 | `distribution` |
| 트렌드 리서치 + 콘텐츠 스크립트 | `curator` |
| 코딩 세션 계획/규율 세팅 | `vibe-coder` |
| 스크립트를 실제 영상으로 | `video-producer` |
| Claude/ChatGPT가 나를 계속 기억하게 하기 | `personal-memory` |
| 게임 제작 | `game-dev` |
| 기존 사업 재무/거래/운영(DCF·RFP·조달 등) | `biz-ops` |
| 글쓰기를 사람 문체로 다듬기 | `writing` |
| SNS 카드뉴스 | `social-carousel` |
| 프롬프트 작성/개선 | `prompt-craft` |
| ERP/SCM/CRM 스펙 전 지식 베이스 | `erp-fundamentals` |
| 다른 곳의 AI 활용 사례를 내 업무에 매핑 | `ai-adoption-scout` |
| 공시(10-K/사업보고서) 분석, 역DCF | `filing-analyst` |
| 상사에게 결정 사항 보고 초안 | `managing-up` |
| 책/문서를 온디맨드 스킬로 변환 | `book-distiller` |
| 이미지 1장/노래로 모션그래픽 뮤직비디오 | `image-motion-graphics` |
| 기존 영상 시청·분석(편집 없이) | `video-watcher` |
| 롱폼 영상을 숏폼 클립으로 | `shorts-clipper` |
| 이미 촬영한 raw 영상 편집(필러컷·색보정·자막) | `footage-editor` |
| 아키텍처/워크플로우 다이어그램을 독립 HTML로 | `diagram-forge` |
| 잠재고객 리서치·자격평가·아웃리치 | `sales-desk` |
| 부트스트랩 창업자 런웨이/번레이트/유닛이코노믹스 | `founder-finance` |

전체 트리거 문구는 `_shared/ROUTING.md`, 스킬별 상세 입출력은 `USAGE.md` 참고.

---

## 자주 묻는 것

**Q. 검증 스크립트(`validate_workspace.ps1`)를 웹에서도 돌려야 하나요?**
아니요. 그건 git 저장소를 로컬에서 편집할 때만 필요합니다. 웹 Project 사용에는 무관합니다.

**Q. `biz-council`이 Reddit/X 데이터를 실시간으로 가져올 수 있나요?**
스킬을 첨부해도 `last30days` 같은 외부 도구가 자동으로 설치되지는 않습니다. 현재 대화의 검색 기능으로 실제 접근한 자료만 근거로 사용하고, Reddit/X 원문이나 최신 데이터를 확인하지 못한 부분은 미확인으로 표시합니다. 검색 기능이 없으면 실시간 리서치 단계는 미실행으로 남깁니다.

**Q. 여러 스킬을 한 번에 첨부해도 되나요?**
가능합니다. 먼저 주 스킬을 정하고, 그 단계가 요구하는 참조 파일부터 첨부하세요.

**Q. 아직 없는 분야(기획/제조 등)를 요청했어요.**
`_shared/DEFERRED.md`에 이미 검토·보류된 근거가 있으면 AI가 그대로 알려줍니다. 새로 만들고 싶다면 GitHub 후보 조사 → 라이선스·내용 확인 → 스킬 작성 → `ROUTING.md` 등록 절차를 따릅니다. Project 파일 첨부와 제품의 스킬 설치 기능은 별개이며, 이 안내는 설치 절차를 다루지 않습니다.

**Q. Claude Code(로컬)와 결과가 다를 수 있나요?**
네. 두 환경의 파일 접근 범위, 설치된 도구, 실행 권한이 다릅니다. 웹에서도 제공되는 기능으로 검색·파일 생성·코드 실행이 가능하지만, 로컬 저장소나 CLI 의존성을 자동으로 사용할 수는 없습니다. 실제 실행한 단계와 문서로만 검토한 단계를 구분해 확인하세요.
