---
name: vibe-coder
description: "Workflow discipline for pair-programming with AI coding agents on your own project: route each task to the right tool/model, gate implementation behind a reviewed plan (Research → Plan → Execute → Review → Ship), keep library docs fresh via Context7, verify before calling anything done. MANDATORY TRIGGERS: 'vibe coding workflow', 'how should I structure this coding session', 'plan mode', 'set up my coding agent workflow', 'which tool for this task'. Distinct from `agent-builder` (autonomous workers, not live pairing). Do not trigger for a single small one-shot edit that doesn't need a plan gate."
allowed-tools:
  - Read
  - Write
  - Bash
  - AskUserQuestion
---

# vibe-coder

"Just prompt it and see what happens" is how solo projects stall at 80% done with an unreviewable pile of code. This skill is not a coding tool itself — it does not write the application code. It's the discipline layer around whichever coding agent is doing the actual work: which tool handles which task, what has to happen before code gets touched, and what has to happen before anything is called finished.

## Attribution

This skill does not reimplement any of these — it composes their converging methodology (all five independently arrive at the same **Research → Plan → Execute → Review → Ship** shape) and names the one MCP tool worth calling out directly.

- **obra/superpowers** — [github.com/obra/superpowers](https://github.com/obra/superpowers), **256,647★** (checked 2026-07-18 via direct GitHub HTML verification), MIT license. Primary reference. Its convergent shape: brainstorm the real spec out loud → chunk the design for human review → write an implementation plan (TDD red/green, YAGNI, DRY) → subagent-driven execution with inspection at each step → verification before completion → code review → finish the branch.
- **affaan-m/ECC** (formerly `everything-claude-code`, same owner, repo renamed) — [github.com/affaan-m/ECC](https://github.com/affaan-m/ECC), 230,819★ (rechecked 2026-07-18), MIT license. Secondary reference (plan → PRD → implement → code review → build-fix → e2e testing → PR) — largest by agent/command/skill count (67/139/278), useful when a task needs one of its more specialized commands rather than superpowers' leaner set. Its own README (fetched and read directly 2026-07-19, not from a secondary source) documents three `settings.json` values worth naming concretely for Step 4's context/cost discipline: `MAX_THINKING_TOKENS: 10000` (down from a 31,999 default, ~70% reduction in hidden-thinking token cost), `CLAUDE_CODE_SUBAGENT_MODEL: haiku` (route spawned subagents to a cheaper model by default, consistent with this skill's fresh-isolated-context principle above), and defaulting `model: sonnet` rather than always reaching for the most expensive option. Its companion **affaan-m/agentshield** ([github.com/affaan-m/agentshield](https://github.com/affaan-m/agentshield), 998★ checked 2026-07-19) scans a Claude Code settings/plugin configuration for security issues (`npx ecc-agentshield scan`) — worth naming when a Step 1 harness setup should be checked rather than assumed safe, though its evidence tier is far thinner than ECC itself.
- **mattpocock/skills** — [github.com/mattpocock/skills](https://github.com/mattpocock/skills), 176,500★ (rechecked 2026-07-19 via GitHub API, now v1.1.0 — renamed `to-prd`→`to-spec` and `to-issues`→`to-tickets` since the 175,644★/2026-07-18 count last cited here), MIT license. Tertiary reference — spec → tickets → TDD → prototype → bug diagnosis → implement → code review → architecture improvement; useful for its explicit `diagnosing-bugs` and `codebase-design` steps that the other two don't name separately. Its v1.1 `wayfinder` skill (plans large work as shared investigation tickets on the issue tracker, replacing an unstructured "just chunk it" approach for scope too big for a single plan artifact) is the reference for Step 3's chunking sub-step when a task is too large for one plan. A third-party walkthrough (Tech Bridge, 6,880 subscribers, 2026-07-18) demonstrates the real command flow (`npx` install → `grill-with-docs` interview to refine requirements → `to-spec`/`to-tickets` to split multi-session work → `implement` → sub-agent code review) and names a "smart zone" of roughly 140k tokens as the context-window budget before a session should be split — `[LOW-EVIDENCE]`, reported by a secondary tutorial channel, not confirmed against Matt Pocock's own docs in this pass, so treat the specific token number as indicative rather than exact. A second video from the same channel (2,874 views, 2026-07-14) covers the v1.1 release itself: its `grilling` (interview) skill gained an explicit confirmation step before moving to implementation, independent validation for this skill's own Step 3 item 5 (don't start execution without sign-off); and its `code-review` skill now checks for Martin Fowler's refactoring-smell categories (e.g. message chains, data clumps) by name, worth naming explicitly if a user's own code-review pass needs concrete smell categories rather than a vague "look for problems."
- **github/spec-kit** — [github.com/github/spec-kit](https://github.com/github/spec-kit), 121,999★ (checked 2026-07-18), MIT license. GitHub's own official spec-driven-development toolkit (`/speckit.specify` → `/speckit.plan` → `/speckit.tasks` → `/speckit.implement`) — reference when a project wants a more formal, checkpointed spec artifact than superpowers' conversational chunking produces.
- **Fission-AI/OpenSpec** — [github.com/Fission-AI/OpenSpec](https://github.com/Fission-AI/OpenSpec), 61,506★ (checked 2026-07-19 via direct GitHub HTML verification), MIT license (verified via raw `LICENSE` file). Second-ranked spec-driven-development alternative to spec-kit per LAW 1 — smaller by star count but a distinct four-stage folder-based lifecycle (`explore` → `propose` → `apply` → `archive`, each proposal materialized as its own directory of spec/design/task-checklist files rather than a single doc) and claims 25+ tool integrations (Claude Code, Cursor, and others) rather than being GitHub-first-party-specific. Choose spec-kit for the higher-evidence/first-party-official pick; choose OpenSpec when the folder-per-change archive trail or the broader tool-integration list specifically matters.
- **upstash/context7** — [github.com/upstash/context7](https://github.com/upstash/context7), 59,283★ (checked 2026-07-18), MIT license. The MCP server this skill names for Step 4 (keeping library docs current) — named directly in the podcast source (Chris Raroque, 2025-12-02) as "Context 7." Not installed by this skill; the user configures it as an MCP server if they want it.
- **Yeachan-Heo/oh-my-claudecode (OMC)** — [github.com/Yeachan-Heo/oh-my-claudecode](https://github.com/Yeachan-Heo/oh-my-claudecode), 37,873★ (checked 2026-07-19 via direct GitHub HTML verification), MIT license (verified via raw `LICENSE` file). A Claude Code plugin, not a separate harness — extends this skill's Step 1 "Skills" building block with 30 specialized agents and named execution modes: `autopilot` (plan→implement→test→verify unattended), `ralph` (repeats until stated acceptance criteria are met, auto-generating a requirements doc first), `ulw` (dispatches more than 5 agents in parallel for bulk work), and `/deep-interview` (a Socratic-questioning pass to pin down requirements before building — the same purpose as this skill's own Step 3.2 interview technique, packaged as a slash command). Surfaced via a Korean-language review (민둥이, 2,598 views, 2026-05-30) — thin channel authority, but the repo's own evidence (star count, MIT license, direct install instructions) stands independently of the reviewer.
- **anomalyco/opencode** (formerly `sst/opencode`, repo transferred — old URL 301-redirects) — [github.com/anomalyco/opencode](https://github.com/anomalyco/opencode), 187,256★ (checked 2026-07-19 via direct GitHub HTML verification), MIT license (verified via raw `LICENSE` file on the `master` branch — no `LICENSE` file exists on `main`, use `master` when checking this repo directly). A terminal-based coding-agent runtime, cited for Step 4's parallel-execution option: a Korean-language case study (에피코딩, 17,852 views, 2026-02-16) ran 7 parallel OpenCode sessions, each given a distinct role, to build roughly 20 small hyper-casual web games in 36 minutes with Claude Max — a self-reported single-session demo, not a maintained benchmark, but real evidence that many-agents-in-parallel is a viable pattern for high-volume, low-interdependency output (many small distinct artifacts), distinct from this skill's git-worktree note above (which is about isolating a few concurrent *related* work streams, not fan-out to dozens of independent ones).
- **shanraisshan/claude-code-best-practice** — [github.com/shanraisshan/claude-code-best-practice](https://github.com/shanraisshan/claude-code-best-practice), 62,989★ (checked 2026-07-18), MIT license. Primary reference for Step 1's four harness components (its own CONCEPTS table names Commands/Rules/Skills/Hooks/MCP as the distinct building blocks) — beyond just the discovery-path role credited elsewhere in this file.
- **DietrichGebert/ponytail** — [github.com/DietrichGebert/ponytail](https://github.com/DietrichGebert/ponytail), 85,215★ (checked 2026-07-18), MIT license. Reference for Step 3's YAGNI emphasis — a dedicated, measured skill for minimal-code discipline ("the best code is the code you never wrote"; ~54% less code on average, up to 94%, per its own benchmark against a fair agentic baseline — cited as its claim, not independently re-verified here).
- **thedotmack/claude-mem** — [github.com/thedotmack/claude-mem](https://github.com/thedotmack/claude-mem), 87,760★ (checked 2026-07-19 via direct GitHub HTML verification), Apache License (verified via raw `LICENSE` file — full SPDX variant not yet confirmed). A Claude Code plugin for persistent cross-session memory, surfaced from a "6 skills" roundup (Nate Herk, 392,938 views, 2026-05-03) alongside already-cited `superpowers` and `get-shit-done` in the same list. Star count alone would rank it above several existing memory references in `personal-memory`/`agent-builder`'s knowledge-graph entries — not yet cross-evaluated against those in this pass; flag as a candidate worth comparing directly (what it persists, how it's queried) before treating it as a ranked recommendation here.
- **mksglu/context-mode** — [github.com/mksglu/context-mode](https://github.com/mksglu/context-mode), 19,071★ (checked 2026-07-19 via direct GitHub HTML verification), **Elastic License 2.0 (ELv2) — source-available, not open source** (verified via raw `LICENSE` file; same "source-available ≠ open source" rule this workspace already applies to `remotion-dev/remotion`, tag `[LICENSE-UNCONFIRMED]` for reference/read only, do not vendor). Surfaced from the same Nate Herk roundup as a context-window-management plugin ("the other half of the context problem," positioned as a companion to `/compact`) — reference/read only under its current license.
- **JuliusBrussee/caveman** — [github.com/JuliusBrussee/caveman](https://github.com/JuliusBrussee/caveman), 90,347★ (checked 2026-07-18), MIT license. Reference for Step 4's output-token economy — cuts reply verbosity (~65% fewer output tokens per its own benchmark) while keeping code/commands/errors byte-exact; distinct from Context7 (which is about input-context freshness, not output length). Surfaced alongside two further token-economy tools in a token-optimization video (Austin Marchese, 2026-07-18): **rtk-ai/rtk** ([github.com/rtk-ai/rtk](https://github.com/rtk-ai/rtk), 71,738★ checked 2026-07-19 via direct GitHub HTML verification, Apache License on its `master` branch — no `LICENSE` on `main`) for input compression, and **teamchong/pxpipe** ([github.com/teamchong/pxpipe](https://github.com/teamchong/pxpipe), 6,470★ checked 2026-07-19, MIT license) for the video's "images instead of text" technique — rendering large structured content as an image for the model to read rather than raw text tokens. Not yet cross-evaluated against caveman for overlap/complementarity in this pass. The same video also named an `openai/codex-plugins` repo that could not be located at that path (404) — treat that specific citation as unverified rather than assuming a typo'd URL.
- **Egonex-AI/Understand-Anything** — [github.com/Egonex-AI/Understand-Anything](https://github.com/Egonex-AI/Understand-Anything), 74,921★ (checked 2026-07-18), MIT license. Reference for Step 3.1's fast-onboarding option — a multi-agent pipeline that builds an interactive knowledge graph of an unfamiliar codebase (files, functions, classes, dependencies) instead of reading files one at a time to reconstruct the architecture.
- **gsd-build/get-shit-done** — [github.com/gsd-build/get-shit-done](https://github.com/gsd-build/get-shit-done), 64,762★ (checked 2026-07-18 via direct GitHub HTML verification), MIT license. **Archived by its owner on 2026-06-26 (read-only; active development continues as "GSD Core" under the Open GSD repository) — cited here for its context-isolation methodology only, not as an actively maintained tool to install.** Reference for Step 4's context-isolation technique — solves "context rot" (quality degradation as a long session's context window fills) by giving each spawned subagent a fresh, isolated context window and handing off between phases via a structured plan artifact rather than shared conversation history.
- **Thariq Shihipar, "A Field Guide to Fable: Finding Your Unknowns"** — (X article + AI Engineer World's Fair keynote, 2026-07-07; verified against Anthropic's own republished blog post, 2026-07-19) — not a GitHub repo; source of Step 3's blind-spot-pass/brainstorm/interview/reference pre-plan techniques and Step 5's implementation-notes-on-deviation and human-comprehension-check additions. Named Anthropic Claude Code technical staff, cited as a technique source rather than an OSS methodology.
- **Pan et al., "Natural-Language Agent Harnesses"** (Tsinghua University, arXiv:2603.25723, 2026-03; link verified live 2026-07-19) and **Lee et al., "Meta-Harness: End-to-End Optimization of Model Harnesses"** (Stanford University, arXiv:2603.28052, 2026-03; link verified live 2026-07-19) — primary papers, not GitHub repos, cited via a PY channel review video (153,962 views, 2026-04-14) whose own reference list resolved to both real arXiv pages. Evidence for Step 1's "start small" guidance: on SWE-bench, a full vs. a stripped-down harness reached the same ~75% pass rate while the bloated one used 14x more compute; an ablation found adding a verification tool *reduced* OSWorld score by 8.4 points; migrating control logic from Python into a natural-language harness raised accuracy from 30.4% to 47.2%; and a harness optimized for one model (Meta-Harness) transferred its gains to five other models, showing the harness — not the underlying model — was the reusable asset. These are the video's own reported numbers, re-stated here from its cited papers rather than independently reproduced.
- **The AI Automators, "Andrej Karpathy's Math Says Agent Skills Will Fail. Build This Instead."** — (YouTube, 96,303 views, 2026-03-21; linked repo [github.com/theaiautomators/claude-code-agentic-rag-series](https://github.com/theaiautomators/claude-code-agentic-rag-series), 247★ checked 2026-07-19 via GitHub API, no `LICENSE` file — `[LICENSE-UNCONFIRMED]`, reference/read only, do not vendor) — cited for its own 12-point harness-design checklist (architecture, planning, filesystem, sub-agent delegation, tool calling, memory, state machine, code execution, context management, human-in-the-loop, validation loops, skills), which extends this skill's Step 1 harness building blocks with a more granular list for a specialized multi-stage harness (their example: an 8-step contract-review pipeline). The video attributes a "March of Nines" reliability argument (a 10-step pipeline at 90% success per step fails multiple times a day) to Andrej Karpathy — `[ASSUMPTION - unverified, confirm with user]`, no primary Karpathy source was found/checked in this pass, cite the underlying reliability-math point on its own merit rather than the attribution.
- **Andrej Karpathy, original "vibe coding" post** — (X, 2025-02; verified via Wikipedia's "Vibe coding" article and its cited primary tweet text, 2026-07-19) — not a GitHub repo; the term's actual coinage, cited to ground this skill's own name and stance. Karpathy's own definition: "fully give in to the vibes... I 'Accept All' always, I don't read the diffs anymore... It's not too bad for throwaway weekend projects, but still quite amusing." He explicitly scoped it to disposable prototypes, not production work. Simon Willison's widely-cited rebuttal (via the same Wikipedia article): "If an LLM wrote every line of your code, but you've reviewed, tested, and understood it all, that's not vibe coding in my book — that's using an LLM as a typing assistant." This skill exists in the gap between those two positions: it is not anti-vibe-coding (Step 2's fast look-tweak-look routing for UI/animation work is Karpathy's mode, deliberately kept), but it gates anything meant to persist past a throwaway session behind Step 3's reviewed plan — the same distinction Karpathy and Willison are drawing, applied as an actual workflow rule rather than left to judgment call each time.
- **Pi Coding Agent** — [pi.dev](https://pi.dev), proprietary/hosted harness, not itself an open-source repo (LAW 1's star-count ranking does not apply — noted here as `[LICENSE-UNCONFIRMED]`, reference/discuss only, nothing to vendor). Source of the "Pi Coding Agent as an alternative harness" note below. Corroborated by three independent videos: IndyDevDan (2026-05-18, "Pi to Pi") demoing peer-to-peer agent comms; Maximilian Schwarzmüller (93,116 views, 2026-03-12) covering its minimal system prompt and non-coding uses (bash tool control, custom skills, financial research); and Ben Holmes/Warp (33,731 views, 2026-05-22) covering the "slash-tree" conversation-branching feature — three separate channels describing the same real tool is enough to treat it as a genuine harness, not a one-video curiosity, though none of the three is a primary spec/doc. The one open-source artifact from this cluster is **disler/pi-vs-claude-code** ([github.com/disler/pi-vs-claude-code](https://github.com/disler/pi-vs-claude-code), 1,510★ checked 2026-07-19 via GitHub API, MIT), an extension repo (`comms`/`comms-net`) implementing the peer-to-peer pattern over Unix sockets + a Bun server.

## Core Laws

Follow `../_shared/CORE-LAWS.md` in full. If the active project has an `AGENTS.md` or equivalent with stricter verification rules, follow those too; otherwise LAW 3 supplies the complete verification-loop, evidence, regression-guard, and HOLD contract. The steps below add task-to-tool routing, plan-gate mechanics, and the specific tools (Context7, git worktrees) that implement the discipline.

## Step 1: Build the harness deliberately (once per project, not once per task)

"Harness" is the environment scaffolding around the model — per `claude-code-best-practice` (see Attribution), it decomposes into four distinct building blocks. Set these up deliberately rather than letting them accumulate ad hoc:

- **Commands** — reusable prompt templates for a repeated request shape (`.claude/commands/<name>.md` or equivalent). If the same multi-paragraph prompt gets typed more than twice, it should be a command.
- **Rules** — the persistent context/constraints file (`CLAUDE.md`/`AGENTS.md`) loaded every session — see Step 3.1 below, this is where "read existing project context first" comes from.
- **Skills** — packaged, triggerable procedures for a recurring workflow shape (this very file is one) — reach for a skill over a command when the procedure has multiple steps/branches, not just a filled-in template. Design each skill as **stateless or stateful deliberately**, not by default (distinction named directly by Matt Pocock himself — see Attribution, `mattpocock/skills` — in a first-party video, 124,800 views, 2026-06-09, about his own `/teach` skill): most skills in this workspace are stateless (read the request, do the task, done), but a skill meant to resume across sessions (his own example: a personalized tutor that tracks a learner's progress and adjusts difficulty to their zone of proximal development) needs its own persistence file and an explicit resume-from-state step, the same discipline this skill's own memory-policy thinking (see `agent-builder` Step 1.4) applies to a worker.
- **Hooks** — enforcement that doesn't depend on the model remembering to do something (e.g. a pre-commit check, a guard against editing a protected path) — use a hook, not a rule-file reminder, for anything that must never be skipped; a rule can be forgotten, a hook can't be.

**Start small and scale in steps** — don't try to build all four at once for a new project. Add a command when you notice repetition, a hook when you notice a rule getting silently ignored, a skill when a command's single template stops covering the branching the task actually needs. A harness built incrementally against real friction beats one designed upfront against imagined friction — this is not just intuition: two 2026-03 papers (see Attribution, Pan et al. and Lee et al.) found a bloated harness matched a stripped-down one's SWE-bench pass rate at 14x the compute cost, and that adding a verification tool can *reduce* an agent's OSWorld score, so treat every added piece of harness structure as a claim to justify, not a default to reach for.

## Step 2: Route the task to the right tool/model before starting

Don't default to one tool for everything. Classify the task first:

- **Broad architecture, new scaffolding, or anything where getting the shape right matters more than speed** — route to the most capable model available, in plan mode, before any code is written.
- **A gnarly multi-step bug, or a change that touches many files in ways that are hard to hold in your head** — route to whichever available agent has the strongest plan-mode/diff-review loop for that kind of change; don't brute-force it with a single large prompt.
- **UI/animation/visual micro-iteration** — route to whichever tool gives the fastest look-tweak-look loop (live preview, screenshot-in-the-loop); this is a different bottleneck (visual judgment, not logic) than the other two.

If unsure which category a task falls in, ask (AskUserQuestion) rather than guessing — the classification determines the next three steps.

**Which mode actually applies here** (see Attribution, Karpathy's original "vibe coding" post): the UI/animation case above is deliberately the one category still run in Karpathy's original "give in to the vibes, don't read the diffs" mode — fast, disposable iterations where the cost of a wrong attempt is one more look-tweak-look cycle, not a shipped defect. Everything else in this skill assumes the opposite: code that's meant to persist gets Step 3's plan gate precisely because "accept all, don't read the diffs" is what Karpathy himself scoped to throwaway weekend projects, not what Willison calls actually reviewed, tested, understood work. Naming which mode a task is in before starting avoids the failure mode of treating a real feature like a disposable prototype (skipped review) or a genuine one-off experiment like it needs a full plan document (wasted ceremony).

**Pi Coding Agent as an alternative harness** (see Attribution): a minimal, extensible harness distinct from Claude Code/Codex, relevant to this step's tool choice for two reasons — a **minimal system prompt** that leaves more room for custom skills and non-coding tasks (bash-level system control, financial-data research) rather than a coding-specific prompt, and a **peer-to-peer agent-comms extension** (`comms`/`comms-net`, see Attribution) where multiple agents ping each other directly with no orchestrator, instead of the hierarchical delegation this skill assumes elsewhere. Route to Pi specifically when the task needs either of those two properties; for ordinary single-session pairing, the tool choice in this step still applies as written. The P2P pattern itself is a multi-agent-operations concern, not a pairing one — see `agent-builder` Step 5 for the fuller evidence/tradeoff writeup (it's evidenced far more thinly than that step's default, Paperclip).

## Step 3: Plan gate — nothing gets implemented without a reviewed plan first

Per the converged shape across all four primary references (see Attribution):

1. **Read existing project context first** (`CLAUDE.md`/`AGENTS.md`, any prior plan artifact from a previous Step 3 run) — a plan written without checking what's already established repeats decisions or contradicts them. For a large or unfamiliar codebase where reading files one at a time doesn't build a full picture fast enough, build a knowledge-graph map first (see Attribution, `Understand-Anything`) rather than guessing at the architecture from a partial read. Get the real spec out loud next — what is this actually trying to do, not just the literal request text.
2. **Surface unknowns before committing to a plan** (see Attribution, Thariq's "Field Guide to Fable"): run a **blind-spot pass** — ask the agent what it doesn't know it doesn't know about this codebase/domain before assigning the work, not after; for a genuinely undecided design/aesthetic direction, request a small **brainstorm/prototype set** (e.g. several distinct directions) to react to instead of describing a preference in words; run an **interview** — have the agent ask one question at a time, prioritizing questions whose answer would actually change the architecture, over stating unstated assumptions yourself; and give a concrete **reference** (existing working code, even in a different language) when the desired behavior is easier to show than to specify.
3. Chunk the design into review-sized pieces the user can actually read and sign off on — not one giant unreadable plan dump. When the work is too large for a single plan artifact (a multi-week initiative, not a single session's task), track it as shared investigation tickets on the issue tracker instead (see Attribution, `mattpocock/skills`' `wayfinder`) rather than forcing an oversized plan into one document.
4. Write the implementation plan explicitly emphasizing **TDD (red/green)**, **YAGNI**, and **DRY** — these three constraints are what keep an autonomous multi-hour execution run from drifting.
5. Do not start execution until the user has signed off on the plan. A plan nobody reviewed is not a gate, it's a formality.

## Step 4: Execution discipline

- **Keep docs current via Context7** (see Attribution) if configured — an agent working from stale training-data knowledge of a library's API is a common, avoidable failure mode. Check whether it's actually set up as an MCP server before assuming it's available; if not, say so rather than silently proceeding on possibly-outdated API assumptions.
- **Run long-lived processes (dev servers, watchers) as background tasks** so the agent can read logs directly during debugging instead of the user copy-pasting terminal output back and forth.
- **Use git worktrees to isolate parallel work streams** when more than one task is running concurrently — prevents one in-flight change from corrupting another's working tree.
- **Subagent-driven execution with inspection at each step**, not one uninspected end-to-end run — per superpowers' model, review happens as each engineering task completes, not only at the very end.
- **Give each subagent a fresh, isolated context window rather than sharing the bloated main session** (see Attribution, `get-shit-done`) — a long-running main session degrades ("context rot") as it fills; a subagent spawned with only the specific task's context stays reliable even when the orchestrating session is large. Hand off between phases with a structured artifact (the plan from Step 3), not by relying on the subagent inheriting the full conversation history.
- **Keep replies terse once the plan is approved** (see `caveman`, Attribution) — full prose explanations of every edit cost output tokens for no benefit once the human has already signed off on the plan; code, commands, and error text stay byte-exact, only the surrounding narration shrinks.

## Step 5: Verification + review gate before calling anything done

Run LAW 3's verification loop and any stricter active-project rules. Also run an actual code-review pass (self-review against the plan from Step 3, or an available AI review tool) before the branch is considered finished, not just "tests pass." A green test suite and a reviewed diff are different checks; both are required.

Two more checks (see Attribution, Thariq's "Field Guide to Fable"): **log implementation notes whenever execution deviates from the Step 3 plan** — don't silently improvise and don't stop to re-litigate the plan for every small deviation, just record what changed and why so the review step isn't reconstructing it from the diff alone. And **verify the human's understanding, not just the agent's output** — a short comprehension check (what did this change do, why this approach) before merging catches the case where the code is correct but the human approving it doesn't actually know what they just shipped.

## Output

When this skill runs at the start of a coding session, it should produce a short plan artifact (following Step 3) — a markdown checklist naming the task classification (Step 2), the chunked design, and the TDD/YAGNI/DRY-constrained implementation steps — for the user to sign off on before any code is touched.

---

## What this skill does not do

It does not write the application code itself — that's the coding agent's job once the plan is approved. It does not install Context7, superpowers, or any of the referenced plugins; it names them and lets the user install what they want. It follows LAW 3 by default and layers any stricter active-project verification rules on top.
