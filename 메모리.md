# skills 프로젝트 — 계정 메모리용 압축본

> `지침.md`(전체판)를 Claude/ChatGPT의 영구 메모리(memory) 기능에 넣기엔 길어서, 핵심만 압축한 버전입니다.
> 이 파일이 저장돼 있으면 매 대화마다 `지침.md`를 다시 첨부하지 않아도 프로젝트 성격을 기억합니다.

---

## 1. 이 프로젝트는 무엇인가

`genie`(라우터) + 14개 전문 스킬, 총 15개로 구성된 AI 스킬 라이브러리. 원래 Claude Code(CLI 에이전트) 전용으로 설계됐고, 이 웹 Project는 같은 `SKILL.md` 파일들을 파일 실행 없이 대화형으로 재사용하는 용도.

## 2. 스킬 요약표 (`_shared/ROUTING.md` 압축)

| 스킬 | 한 줄 | 트리거 예 |
|---|---|---|
| `genie` | 요청을 알맞은 스킬로 안내 (직접 해결 안 함) | "지니야", 스킬명을 모를 때 |
| `biz-council` | 실사용자 리서치 → 아이디어 스코어링 → 5인 위원회 판단 | "이 아이디어 검증해줘" |
| `design-report` | 시각 디자인 방향 + DESIGN.md + 문서(DOCX/PPTX/PDF) 변환, PPTX AI티 제거 2차 보정(Canva 등) | "디자인해줘", "보고서로 만들어줘" |
| `agent-builder` | AI 워커(자동화 에이전트) 설계 + 로컬/클라우드 모델 라우팅, 고정 소규모 파이프라인은 프레임워크 없이 서브에이전트로 | "이 작업 자동화 에이전트 만들어줘" |
| `distribution` | 유통/마케팅 채널 3-5개 구체 제안 | "이거 어떻게 홍보하지" |
| `curator` | 트렌드 리서치 → 정직한 관점 → 스크립트화 | "이 뉴스로 짧은 영상 만들어줘" |
| `vibe-coder` | 코딩 세션 규율(계획 게이트, 도구 라우팅) | "코딩 워크플로우 세팅해줘" |
| `video-producer` | 스크립트 → 실제 렌더링된 영상 | "이걸 영상으로 만들어줘" |
| `personal-memory` | 사람의 세션 간 메모리 구축 단계 진단 | "Claude가 나를 기억하게 하고 싶어" |
| `game-dev` | AI 에이전트가 직접 엔진을 조작해 게임 제작 | "게임 만들어줘" |
| `biz-ops` | 기존 사업의 재무/영업/운영 지원 | "DCF 모델 만들어줘" |
| `writing` | AI 티 나는 글 33패턴 제거, 목소리 매칭 | "이 글 사람이 쓴 것처럼 다듬어줘" |
| `social-carousel` | 트렌드 기반 SNS 카드뉴스 생성 | "카드뉴스 만들어줘" |
| `prompt-craft` | LLM 프롬프트 작성/개선 | "프롬프트 잘 짜줘" |
| `erp-fundamentals` | ERP 업종무관 공통 베이스 + 업종별 표준(ISA-95/GAAP·IFRS/Incoterms·HS/GS1) 매핑 | "ERP 만들어줘", "ERP 스펙 뭐가 필요해" |

없는 분야(기획/제조/판매/재무운영 등)는 `_shared/DEFERRED.md`에 이미 검토·보류 기록이 있음 — 지어내지 말고 그대로 전달.

## 3. 도구 격차 (항상 기억)

이 스킬들의 `allowed-tools`는 거의 전부 `Bash`/`WebFetch`/`WebSearch`/`Task`/`Write`를 전제로 함. 웹 Project엔 이 도구가 없다.

- 파일 저장/스크립트 실행 → 텍스트로 결과 제시, 사용자가 직접 저장
- 실시간 리서치(별점, 트렌드, Reddit/X 데이터) → 호스트 자체 브라우징으로 대체하거나, 없으면 "실시간 확인 불가"라고 정직히 말함. 지어낸 수치를 사실처럼 말하지 않음
- 5인 위원회 등 서브에이전트 → 한 대화 안에서 순서대로 직접 수행
- `validate_workspace.ps1` 등 검증 스크립트 → git 저장소 전용, 웹 사용과 무관

## 4. 요청 처리 순서

1. `_shared/ROUTING.md` 기준으로 요청 분류
2. 해당 스킬의 `SKILL.md`가 첨부돼 있는지 확인 → 없으면 첨부 요청 (기억으로 지어내지 않음)
3. 여러 스킬에 걸치면 후보 제시 후 사용자가 선택
4. 해당 스킬이 없으면 `DEFERRED.md` 확인 후 정직하게 "없다"고 답하고 신규 제작 여부를 물음

## 5. 공유 법칙 (`_shared/CORE-LAWS.md`) — 한 줄 요약

LAW 0 추측 금지 · LAW 1 도구는 실제 별점/라이선스 증거로 채택 · LAW 2 코드화 시 UI/로직 분리·명시적 성공-실패 반환 · LAW 3 검증 루프는 종료조건·HOLD를 갖고 닫혀야 함 · LAW 4 스킬 설명은 트리거 중심, 당연한 말 생략.

## 6. 범위 밖

이 저장소와 무관한 일반 잡담·개인 일정 관리 등은 범위 밖. 범위 밖 요청은 어떤 스킬로도 재해석되지 않으면 그렇다고 말한다.
